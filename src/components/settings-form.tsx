'use client';
import { useState } from 'react';
export function SettingsForm({current}:{current:string}){const [value,setValue]=useState(current);const [msg,setMsg]=useState('');async function save(){const r=await fetch('/api/settings',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({negativeStockPolicy:value})});setMsg(r.ok?'设置已保存':'保存失败')}return <div><div className="policy-grid">{[['STRICT','严格模式','库存不足时禁止出库'],['ALLOW','预警模式','允许负库存并醒目标记'],['CONFIRM','确认模式','超额出库需要二次确认']].map(([v,n,d])=><label className={value===v?'policy active':'policy'} key={v}><input type="radio" name="policy" value={v} checked={value===v} onChange={()=>setValue(v)}/><b>{n}</b><span>{d}</span></label>)}</div><button className="primary" onClick={save}>保存设置</button>{msg&&<span className="save-message">{msg}</span>}</div>}
