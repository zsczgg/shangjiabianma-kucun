# 设置与连续扫码优化设计

API Key 在设置页只允许验证后覆盖，不回显明文。新 Key 使用服务器密钥经 AES-256-GCM 加密后写入独立数据库，环境变量作为故障恢复备用。

低库存阈值采用全局默认值与 SKU 独立覆盖两层模型。默认阈值变更只更新仍在使用默认值的 SKU；独立阈值可随时恢复为全局默认。

扫码页采用连续作业焦点模型：每次识别后清空扫码框，库存操作完成后恢复扫码框焦点；数量输入获得焦点期间暂停全局扫码字符接管，提交或失焦后恢复。
